'use strict';

module.exports = function (stdlib, foreign, heap) {
    'use asm';

    function uniqueCols (mx, geometry) {
        mx = mx >>> 0;
        geometry = geometry >>> 0;
        return;
    }

    function uniqueRows (mx, geometry) {
        mx = mx >>> 0;
        geometry = geometry >>> 0;
        return;
    }

    return {
        uniqueCols: uniqueCols,
        uniqueRows, uniqueRows
    };
}
